# Copy this file to secrets.tfvars and fill in actual values.
# secrets.tfvars is git-ignored — NEVER commit real secrets.

db_password = "REPLACE_WITH_SECURE_PASSWORD"
jwt_secret  = "REPLACE_WITH_JWT_SECRET"
