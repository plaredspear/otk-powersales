/// 주문서 작성 화면의 라인 수 상한.
abstract final class OrderLimits {
  /// 승인요청(정식 등록) 가능한 최대 제품 라인 수 — 서버 검증(`ORD_INVALID_REQUEST`)과 동일 값.
  ///
  /// **제품 담기(추가/바코드)는 이 수를 넘어도 허용한다** — 후보를 넉넉히 담아둔 뒤 추려서
  /// 요청하는 흐름을 막지 않기 위함. 대신 초과 상태에서는 승인요청 버튼이 비활성화되고
  /// 제출 검증에서도 차단된다 (레거시 write.jsp:556 `alert` 와 동일 상한 — 100개 초과 주문이
  /// SAP 등록에서 실패했던 이력 때문에 들어간 제한).
  static const int maxOrderLines = 100;
}
