spring.application.name=otk-pwrs-backend

# ---------------------------------------------------------------------------
# 기본값(local / 테스트): DB/Redis 가 없는 환경에서 auto-config 가 빈 생성을
# 시도해 기동 실패하지 않도록 제외한다. EB 에서는 buildspec 이
# SPRING_PROFILES_ACTIVE=aws 를 주입하여 application-aws.properties 가 해당
# 제외를 해제하고 DataSource/Redis/Secrets Manager 를 활성화한다.
# ---------------------------------------------------------------------------
spring.autoconfigure.exclude=\
  org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration,\
  org.springframework.boot.data.jpa.autoconfigure.HibernateJpaAutoConfiguration,\
  org.springframework.boot.data.redis.autoconfigure.RedisAutoConfiguration,\
  org.springframework.boot.data.redis.autoconfigure.RedisRepositoriesAutoConfiguration

# ---------------------------------------------------------------------------
# Actuator — /actuator/health 만 노출. show-details=always 는 web UI 에서
# DB/Redis 상태 카드를 렌더링하기 위함. EB Load Balancer 헬스체크 경로도 동일.
# ---------------------------------------------------------------------------
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always
management.endpoint.health.probes.enabled=true

# ---------------------------------------------------------------------------
# 앱 구성 값 (모든 스테이지 공통 placeholder). 실제 값은 프로파일 별 파일에서
# 채워지거나 env var 로 주입된다. local 에서는 빈 값 + auto-config 제외.
# ---------------------------------------------------------------------------
app.stage=${STAGE:local}
app.aws.region=${AWS_REGION:ap-northeast-2}
