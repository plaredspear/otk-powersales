CREATE TABLE monthly_female_employee_integration_schedule (
    monthly_female_employee_integration_schedule_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    sfid                                    VARCHAR(18),
    name                                    VARCHAR(80),
    external_key                            VARCHAR(255) UNIQUE,
    year                                    VARCHAR(255),
    month                                   VARCHAR(255),
    account_sfid                            VARCHAR(18),
    employee_sfid                           VARCHAR(18),
    cost_center_code                        VARCHAR(40),
    working_category1                       VARCHAR(255),
    working_category3                       VARCHAR(255),
    working_category4                       VARCHAR(255),
    working_category5                       VARCHAR(255),
    emp_branch_name                         VARCHAR(255),
    professional_promotion_team             VARCHAR(255),
    working_days_month                      NUMERIC(14,4),
    number_of_inputs                        BIGINT,
    equivalent_number_of_working_days       NUMERIC(14,4),
    converted_headcount                     NUMERIC(14,4),
    edi_pos                                 BIGINT,
    this_month_amount                       BIGINT,
    account_converted_headcount             NUMERIC(14,4),
    employee_input_criteria_master_sfid     VARCHAR(18),
    is_deleted                              BOOLEAN,
    employee_id                             BIGINT REFERENCES employee(employee_id),
    account_id                              INTEGER REFERENCES account(account_id),
    created_at                              TIMESTAMP NOT NULL DEFAULT now(),
    updated_at                              TIMESTAMP NOT NULL DEFAULT now()
);
