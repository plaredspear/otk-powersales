# Heroku 데이터 마이그레이션 reset-dev 용 dev DB 접속 정보 템플릿.
# 복사 후 db.properties 작성 (db.properties 는 gitignore).
#
# ⚠️ dev 전용 — reset-dev 는 운영 RDS endpoint 감지 시 거부한다. localhost(SSM 터널)만 허용.
# 사전 조건: scripts/db-tunnel.sh -s dev 로 SSM 터널이 열려 있어야 한다.

jdbc.url=jdbc:postgresql://localhost:15432/otoki?currentSchema=powersales
jdbc.user=otkadmin
jdbc.password=
